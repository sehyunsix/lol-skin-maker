# LoL Skin Maker 셋업 가이드

## 🎯 빠른 시작 (5분)

### Step 1: 저장소 준비
```bash
cd /Users/yuksehyun/project/lol-skin-maker
```

### Step 2: 패키지 설치
```bash
npm install
```

설치 중 다음 패키지들이 설치됩니다:
- `express`: 웹 서버 프레임워크
- `multer`: 파일 업로드 처리
- `sharp`: 이미지 처리 (이미지 변환, 합성)
- `axios`: HTTP 클라이언트
- `dotenv`: 환경 변수 관리
- `nodemon` (dev): 자동 재시작 (개발용)

### Step 3: 환경 변수 설정 (선택사항)
```bash
# .env 파일 생성
cat > .env << EOF
PORT=3000
NODE_ENV=development
# NANOBANA_API_KEY=your_key_here (선택사항)
EOF
```

### Step 4: 서버 실행
```bash
# 개발 모드 (추천)
npm run dev

# 또는 프로덕션 모드
npm start
```

### Step 5: 브라우저에서 열기
```
http://localhost:3000
```

---

## 🔧 상세 설정

### 환경 변수 설정

#### `.env` 파일 생성
프로젝트 루트에 `.env` 파일을 생성합니다:

```env
# 서버 설정
PORT=3000
NODE_ENV=development

# 나노바나 API 설정 (선택사항)
NANOBANA_API_KEY=your_api_key_here_if_you_have
```

#### 나노바나 API 키 없이 시작
API 키가 없어도 로컬 이미지 처리로 자동 동작합니다.
- 🟢 **추천**: 별도의 API 가입 불필요
- 🟡 **제약**: 기본적인 이미지 변환만 가능

#### 나노바나 API와 함께 사용
고급 이미지 생성이 필요하면:

1. [나노바나](https://nanobana.com) 가입
2. 개발자 콘솔에서 API 키 생성
3. `.env` 파일에 `NANOBANA_API_KEY=...` 추가
4. 서버 재시작

---

## 📦 패키지 설명

| 패키지 | 용도 | 버전 |
|--------|------|------|
| `express` | 웹 서버 | ^4.18.2 |
| `multer` | 파일 업로드 | ^1.4.5 |
| `sharp` | 이미지 처리 | ^0.33.0 |
| `axios` | HTTP 요청 | ^1.6.2 |
| `form-data` | FormData 처리 | ^4.0.0 |
| `dotenv` | 환경 변수 | ^16.3.1 |
| `nodemon` | 개발용 자동 재시작 | ^3.0.2 |

---

## 🚀 실행 명령어

### 개발 모드 (추천)
```bash
npm run dev
```
- 파일 변경 시 자동 재시작
- 디버깅 정보 출력
- 콘솔 로그 표시

### 프로덕션 모드
```bash
npm start
```
- 최적화된 성능
- 프로덕션 배포용

### 포트 변경
```bash
PORT=3001 npm run dev
```

---

## 🧪 테스트

### 1. 서버 상태 확인
```bash
curl http://localhost:3000/api/health
```

응답 예:
```json
{
  "status": "ok",
  "apiKey": "설정됨",
  "timestamp": "2024-11-20T10:30:00Z"
}
```

### 2. 스킨 생성 테스트
```bash
# 테스트 이미지로 스킨 생성
curl -X POST http://localhost:3000/api/generate-skin \
  -F "image=@test-image.jpg" \
  -F "champion=Ahri"
```

---

## 📁 프로젝트 구조

```
lol-skin-maker/
├── index.html              # 메인 UI
├── server.js              # Express 서버 (핵심)
├── image-processor.js     # 이미지 변환 로직
├── package.json           # 의존성 정의
├── .env.example           # 환경 변수 템플릿
├── .gitignore            # Git 무시 파일
├── README.md             # 프로젝트 설명
├── SETUP.md              # 이 파일
├── uploads/              # 생성된 이미지 저장소
└── node_modules/         # 설치된 패키지
```

---

## 🎨 이미지 처리 흐름

```
사용자 사진 업로드
    ↓
파일 유형 검증
    ↓
이미지 정규화 (308x560)
    ↓
LoL 스킨 스타일 적용
    ├─ 밝기 조정
    ├─ 포화도 증가
    └─ 색상 회전
    ↓
LoL 테마 추가
    ├─ 황금색 테두리
    ├─ 챔피언 이름
    └─ "CUSTOM SKIN" 텍스트
    ↓
이미지 저장 및 반환
```

---

## 🔒 보안 체크리스트

- ✅ 파일 타입 검증 (이미지만 허용)
- ✅ 파일 크기 제한 (10MB)
- ✅ 경로 검증 (디렉토리 탐색 방지)
- ✅ 임시 파일 자동 삭제
- ✅ API 키는 환경 변수로 관리

---

## 🐛 자주 발생하는 문제

### 1. "포트 3000이 이미 사용 중입니다"
```bash
# 다른 포트 사용
PORT=3001 npm run dev

# 또는 기존 프로세스 종료
lsof -i :3000
kill -9 <PID>
```

### 2. "Sharp 설치 실패"
```bash
# 의존성 재설치
npm install --build-from-source
```

### 3. "uploads 디렉토리 오류"
```bash
# 디렉토리 생성 (서버가 자동으로 생성하지만 필요시)
mkdir -p uploads
```

### 4. ".env 파일을 찾을 수 없습니다"
```bash
# .env 파일 생성
touch .env
echo "PORT=3000" >> .env
```

---

## 📊 성능 최적화

### 이미지 크기 제한
현재 설정: **10MB**
- `server.js` 라인 25-26에서 수정 가능

### 동시 업로드 제한
현재 설정: 무제한
- `multer` 설정에서 `limits` 추가 가능

---

## 🌐 배포 가이드

### Heroku 배포 예시
```bash
# heroku CLI 설치 후
heroku create lol-skin-maker
git push heroku main
```

### AWS/Google Cloud 배포
Docker 컨테이너로 배포 권장:
```bash
docker build -t lol-skin-maker .
docker run -p 3000:3000 lol-skin-maker
```

---

## 📞 지원

문제 해결이 필요하면:
1. `npm run dev` 실행 중 콘솔 로그 확인
2. 브라우저 개발자 도구 (F12) 확인
3. `.env` 파일 설정 확인

---

**설정 완료! 이제 스킨을 만들어보세요!** 🎮✨

